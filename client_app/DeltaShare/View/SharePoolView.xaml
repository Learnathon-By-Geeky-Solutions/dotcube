<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:model="clr-namespace:DeltaShare.Model"
    xmlns:viewmodel="clr-namespace:DeltaShare.ViewModel"
    x:Class="DeltaShare.View.SharePoolView"
    x:DataType="viewmodel:SharePoolViewModel"
    Title="SharePoolView">
    <VerticalStackLayout>
        <Label
            Text="Demo QR code test"
            VerticalOptions="Center"
            HorizontalOptions="Center" />
        <CollectionView
            ItemsSource="{Binding PoolUsers}"
            ItemsLayout="HorizontalList"
            SelectionMode="None">
            <CollectionView.ItemTemplate>
                <DataTemplate
                    x:DataType="model:User">
                    <VerticalStackLayout>
                        <Image
                            MaximumHeightRequest="48"
                            Source="dotnet_bot.png" />
                        <Label
                            Text="{Binding Username}"
                            HorizontalOptions="Center" />
                    </VerticalStackLayout>
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>
        <Button
            Text="Generate QR for test"
            Command="{Binding ClickGenerateQRBtnCommand}"
            FontSize="Medium"
            HorizontalOptions="Fill" />
        <Image
            Source="{Binding PoolQRImage}"
            VerticalOptions="Center"
            HorizontalOptions="Center"
            HeightRequest="200"
            WidthRequest="200" />
    </VerticalStackLayout>
</ContentPage>